#include <stdio.h>

int main()
{
    int n,i,j,c=0;
    scanf("%d",&n);
    for(i=n;i>=0;i--)
    {
        c=c+1;
        for(j=c;j<=n;j++)
        {
            printf("%d ",j);
        }
        printf("\n");
    }    
}
