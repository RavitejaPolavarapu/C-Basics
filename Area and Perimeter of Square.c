/// Area and Perimeter of Square
#include<stdio.h>
void main()
{
	//Variable Declaration and Initialization
	int s=8,a=s*s,p=4*s;
	//output
	printf("Area of the Square with side %d is %d\nPerimeter of the Square with Side %d is %d",s,a,s,p);
	}
